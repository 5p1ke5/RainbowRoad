/// @description NPC speech.
if /*!(instance_exists(myBalloon)) &&*/ (dialogueIndex > 0)
{
	event_user(0);
	//var _text = dialogue[dialogueIndex];
	//dialogueIndex++;
	//dialogueIndex = dialogueIndex % array_length(dialogue);
	
	//myBalloon = npc_speak(_text);
}